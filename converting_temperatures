"""
This program uses two functions to convert celcius to farenhieght and 
farenhieght to celsius using two functions.
"""

# Function to convert Celcius to Farenheight
def cel_to_far(cel):
    return round((1.8 * cel) + 32, 2)


# Function to convert Farenheight to Celsius
def far_to_cel(far):
    return round((far - 32) / 1.8, 2)

# Convert celsius to farenheight
celsius_values = [0, 100]
for cel in celsius_values:
    print(f"{cel}°C is equivalent to {cel_to_far(cel)}°F")

#Convert farenheight to celcius
farenheight_values = [40, 80]
for far in farenheight_values:
    print(f"{far}°F is equivalent to {far_to_cel(far)}°C")
